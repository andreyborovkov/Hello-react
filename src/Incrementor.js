import React, {Component} from 'react';

class Incrementor extends Component{
    render(){
        return(
            <div>
                ++
            </div>
        );
    }
}

export default Incrementor;